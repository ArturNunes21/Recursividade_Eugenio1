#ifndef CLASSEITERATIVA_H
#define CLASSEITERATIVA_H
#include <QString>
#include <cmath>
#include <iostream>

namespace Artur{

class classeIterativa
{
public:
    QString imprimirIterativo(int valor);

      QString imprimirParIterativo(int valor);

      QString imprimirImparIterativo(int valor);

      QString imprimirSomaIterativa(int valor);

      double imprimirPiIterativoUm();

      double imprimirPiIterativoDois();
};

}   //namespace
#endif // CLASSEITERATIVA_H
